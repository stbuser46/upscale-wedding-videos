# Cleanup record — 2026-08-04

User-approved deletion of obsolete v1/v2 experiment working files to reclaim
disk space (~526 GB freed). These were regenerable intermediates from the
abandoned RealESRGAN/CodeFormer pipelines, not sources. The finished result
videos of those experiments remain in `out/`
(`YacoobAysha_DVD1_mid10min_cf.mkv`, `YacoobAysha_DVD1_mid10min_v2.mkv`).

## Deleted from `work/`

- `slice10/` (172 GB) and `slice10_v2/` (334 GB) — extracted frame dumps of
  the old 10-minute RealESRGAN/CodeFormer runs
- `smoke/`, `smoke_v2/`, `cf_motion/`, `cf_test/`, `bench/`, `testframes/`,
  `supir_in/`, `supir_out/`, `supir_face_in/` — v1/v2-era test directories
- loose test files: `_bench_16.mp4`, `_bench_32.mp4`, `_full24.mkv`,
  `test_1container.mp4`, `test_clip_upscaled.mp4`, `smoke_cf.mkv`,
  `smoke_v2.mkv`

## Kept (unchanged)

- `source/` — both original ISOs (archival sources of truth)
- `work/dvd1_title.vob` — extracted main title
- `work/midpoint_v3/` and the three `midpoint_smoke_v3*` dirs — current
  v3 pipeline artefacts referenced by `CURRENT_PIPELINE.md`
- `out/` — all finished outputs, including the old experiments' final videos
- `models/` — pinned SeedVR2 model weights
